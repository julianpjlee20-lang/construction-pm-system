# 伺服器設定
PORT=8096
NODE_ENV=development

# Google Drive API 設定（服務帳號）
# 1. 前往 https://console.cloud.google.com
# 2. 建立專案「工程專案管理系統」
# 3. 啟用 Google Drive API
# 4. 建立服務帳號 (Service Account)
# 5. 下載 JSON 金鑰檔，將以下欄位填入：

# 服務帳號 email（從 JSON 金鑰檔取得）
GOOGLE_SERVICE_ACCOUNT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com

# 服務帳號私鑰（從 JSON 金鑰檔取得 private_key 欄位，保留 \n）
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"

# Google Drive 根資料夾 ID（可選）
# 如果不設定，將在你的 Google Drive 根目錄建立「工程專案管理」資料夾
# 如果要指定特定資料夾，請分享該資料夾給服務帳號（編輯權限），並填入資料夾 ID
GOOGLE_DRIVE_FOLDER_ID=

# ========================================
# Google Drive 設定步驟：
# ========================================
# 1. 前往 https://console.cloud.google.com
# 2. 建立新專案「工程專案管理系統」
# 3. 啟用 API：
#    - 搜尋「Google Drive API」
#    - 點擊「啟用」
# 4. 建立服務帳號：
#    - 左側選單 → IAM 與管理 → 服務帳號
#    - 建立服務帳號
#    - 名稱：construction-project-manager
#    - 角色：不需要（我們用資料夾權限控制）
# 5. 建立金鑰：
#    - 點擊剛建立的服務帳號
#    - 金鑰 → 新增金鑰 → JSON
#    - 下載 JSON 檔案
# 6. 設定權限：
#    方案 A：分享特定資料夾
#      - 在 Google Drive 建立資料夾「工程專案管理」
#      - 右鍵 → 共用
#      - 貼上服務帳號 email (xxxxx@xxxxx.iam.gserviceaccount.com)
#      - 權限：編輯者
#      - 複製資料夾 ID（URL 中的最後一段）填入 GOOGLE_DRIVE_FOLDER_ID
#    方案 B：使用根目錄
#      - 留空 GOOGLE_DRIVE_FOLDER_ID
#      - 系統會在服務帳號的 Drive 中自動建立資料夾
#      - ⚠️ 需要用服務帳號登入 Google Drive 才能看到檔案
# 7. 填入環境變數：
#    - 從 JSON 檔案複製 client_email → GOOGLE_SERVICE_ACCOUNT_EMAIL
#    - 從 JSON 檔案複製 private_key → GOOGLE_PRIVATE_KEY（保留 \n）
# ========================================

# 測試連線：
# curl http://localhost:8096/health
